| ID | Phase | Status | Depends on | Brief | Started | Completed | Notes |
|----|-------|--------|------------|-------|---------|-----------|-------|
| 0 | Decide auth scope + register OAuth client | complete | — | phase-00-scope-and-client.md | 2026-05-03 | 2026-05-03 | OAuth client registration is operator-driven (separate repo + running auth service); decisions.md documents the procedure and target values. Secret stays out of repo. |
| 1 | Add OIDC config + JWKS verifier helpers | complete | 0 | phase-01-oidc-helpers.md | 2026-05-03 | 2026-05-03 | jose@^6.2.1 added; jwks.test.ts covers happy / wrong-iss / wrong-aud / expired / bad-signature. Pre-existing lint errors in unrelated files left untouched; tests in `src/lib/billing/*` are unrelated WIP and out of scope. |
| 2 | Implement n450s login + callback routes | complete | 1 | phase-02-login-callback.md | 2026-05-03 | 2026-05-03 | setSessionTokens left as a no-op stub in callback route — phase 3 wires real cookie writes. HMAC sign/verify helpers duplicated across login/callback (no shared module added since it would fall outside the brief's "Files to touch" allowlist). |
| 3 | Token storage + middleware refresh (sliding sessions) | complete | 2 | phase-03-token-refresh.md | 2026-05-03 | 2026-05-03 | Helper + middleware land here; brief's "Files to touch" omits the callback route, so the phase-2 setSessionTokens stub still no-ops — wiring it to call the new helper is required before sessions actually persist (move to phase 4 or a follow-up). Middleware enforces login redirect only on `/` and `/dashboard/*`; `/api/*` passes through and relies on the future `auth()` wrapper. |
| 4 | Rewrite `auth()` wrapper around n450s tokens (advanced mode) | complete | 3 | phase-04-auth-wrapper.md | 2026-05-03 | 2026-05-03 | advanced-mode `auth()` resolves sessions from `s5_at` via session-resolver (sub used as user.id with TODO until phase 5). NextAuth no longer invoked from auth(); `handlers` export kept so the legacy `/api/auth/[...nextauth]` route survives until phase 6 (route stub change is outside this brief's "Files to touch" allowlist). Phase-3 carryover still present: callback route's `setSessionTokens` remains a no-op stub, so login does not yet persist cookies — needs a follow-up before manual verification can pass end-to-end. Pre-existing tsc errors in `src/lib/authorization.test.ts` and unrelated lint errors left untouched. |
| 5 | User-row linking (`User.authIdentityId = sub`) | complete | 4 | phase-05-user-link.md | 2026-05-03 | 2026-05-03 | `User.authIdentityId` added (sparse unique) + plumbed through both adapters; n450s callback now resolves-or-creates the local user (sub → email-link → create). Migration script `scripts/link-existing-users-to-n450s.ts` calls `/admin/api/users-sync` with `N450S_ADMIN_TOKEN`, dry-run by default, idempotent on re-run. Pre-existing tsc errors in `src/lib/authorization.test.ts` and `group-card-actions.tsx` left untouched. Carryover: `session-resolver.ts` still surfaces `sub` as `user.id` (its phase-5 TODO) — outside this brief's "Files to touch", so the local `User._id` swap belongs to phase 6/7 wiring. Phase-3 callback-cookie no-op also still present. |
| 6 | Remove Credentials + Magic-invite providers | complete | 4,5 | phase-06-deprecate-providers.md | 2026-05-03 | 2026-05-03 | NextAuth + Credentials + MagicInvite providers + MongoDBAdapter dropped from src/lib/auth.ts; legacy /api/auth/[...nextauth] and /api/register routes deleted; register page redirects to AUTH_SERVICE_URL/signup; group-invite email wraps acceptInviteUrl in n450s signup URL (invite_code = trailing token, continue = full URL — best-effort since sub5tr4cker doesn't yet mint real n450s invite codes). Carryover: `next-auth` package kept (still used by `(auth)/login/login-form.tsx`, `(auth)/invite-callback/page.tsx`, `components/layout/app-sidebar.tsx` which call signIn/signOut from "next-auth/react" — phase 7 territory; those calls will fail at runtime now that the [...nextauth] route is gone). `bcryptjs` kept (still used by `src/app/api/user/change-password/route.ts`). `createMagicLoginToken` kept in tokens.ts because `src/lib/telegram/handlers.ts` still imports it (out of this brief's "Files to touch"); only `verifyMagicLoginToken` removed. `pnpm-lock.yaml` regenerated to drop `@auth/mongodb-adapter` (staged with the dep change). Pre-existing tsc errors in `group-card-actions.tsx`, `group-detail-admin-actions.tsx`, `authorization.test.ts` left untouched. |
| 7 | Login UI rewrite (n450s redirect, Google via n450s, fallback states) | complete | 4 | phase-07-login-ui.md | 2026-05-03 | 2026-05-03 | Login page reads SUB5TR4CKER_MODE server-side; LoginForm renders a single "Continue with n450s" CTA in advanced mode and an auto-redirect-to-dashboard in local mode. Sidebar sign-out swapped from `signOut()` (next-auth) to a plain link → `/api/auth/n450s/logout?post_logout_redirect_uri=/`. `app-header.tsx` left untouched — it has no sign-out UI to wire (only breadcrumbs / titles); flagged here so the brief's "Files to touch" allowlist is honoured. Carryover: (a) local-mode sign-out is effectively a no-op — the `/api/auth/n450s/logout` route calls `getN450sAuthConfig()` which throws when n450s env vars are absent. Properly satisfying the brief's "local logout in local mode" needs either a mode branch in the logout route or a separate `/api/auth/local/logout` endpoint — both outside this brief's allowlist. (b) `src/app/(auth)/invite-callback/page.tsx` still imports `signIn from "next-auth/react"` and will break at runtime now that NextAuth is gone — outside this brief's allowlist (phase 6 carryover, belongs in phase 8 or a follow-up). (c) Pre-existing lint errors in unrelated files (dist/cli/index.js, scripts/copy-standalone-assets.js, theme-toggle.tsx, callback/route.ts, plugins/*, etc.) left untouched. |
| 8 | Tests + integration smoke | complete | 5,6,7 | phase-08-tests.md | 2026-05-03 | 2026-05-03 | 22 unit tests added across session-resolver / session-cookies / middleware (all green; 93/93 suite). Smoke checklist deferred to operator at cutover (phase 9) since it needs a running n450s_auth instance. Pre-existing phase-3/7 carryovers (callback `setSessionTokens` still stub, local-mode logout no-op, invite-callback signIn import) flagged in results doc; outside this brief's allowlist. |
| 9 | Production cutover + rollback plan | complete | 8 | phase-09-cutover.md | 2026-05-03 | 2026-05-03 | Runbook + rollback procedure shipped in `phase-09-cutover-log.md`. Production cutover itself is operator-driven and not yet executed — operator fills the run-log table at cutover time. Pre-flight blockers surfaced (must close before flipping traffic): (a) callback `setSessionTokens` is still the phase-2 no-op stub — login does not yet persist cookies end-to-end, smoke item #1 will fail until fixed; (b) `src/app/(auth)/invite-callback/page.tsx` still imports `signIn from "next-auth/react"` (phase-6 carryover). Both are outside this brief's "Files to touch" allowlist; documented for the follow-up. CHANGELOG bumped 0.38.5 → 0.39.0 (minor: new login flow, removed NextAuth route + register route + Google button); `_context/context.md` updated to reflect n450s_auth in advanced mode and the new auth route surface. `pnpm-lock.yaml` does not embed the project version, so no lockfile edit. |
