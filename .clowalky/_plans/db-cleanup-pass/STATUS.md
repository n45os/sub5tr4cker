| ID | Phase | Status | Depends on | Brief | Started | Completed | Notes |
|----|-------|--------|------------|-------|---------|-----------|-------|
| 0 | Inventory: enumerate cleanup candidates from prod DB | complete | — | phase-00-inventory.md | 2026-05-03 | 2026-05-03 | inventory written into the brief itself (see file). Counts: 6 orphan billingperiods (all under inactive group `69bac59addf7b647fbfdbde4`); 2 duplicate-billingperiod buckets (defer to `fix-duplicate-billing-periods/4` cleanup script); 0 across orphan-tasks / old-notifications / stale-locks / old-audit-events. Phase 1 should target exactly one mutation: archive the 6 orphan billingperiods. operator-advanced via advance-operator-blocks. |
| 1 | Build a `dbcleanup` CLI script with dry-run as default | complete | 0 | phase-01-cleanup-script.md | 2026-05-03 | 2026-05-03 | harness only — passes are stubs as briefed; no new StorageAdapter methods needed yet (deferred to phases 2/3/4 when real logic lands). `pnpm db:cleanup` runs `tsx scripts/db-cleanup.ts`. `--apply` without a pass flag exits 2. |
| 2 | Pass 1: cancel scheduled tasks pointing at deleted groups/periods | complete | 1 | phase-02-orphan-tasks.md | 2026-05-03 | 2026-05-03 | implemented in scripts/db-cleanup.ts via mongoose models + raw collection writes (no adapter changes — `cancellationReason` not in ScheduledTask schema, `scheduled_tasks_orphan_cleanup` not in AuditEvent enum, both bypassed via `.collection.updateMany/insertOne` to keep schemas unchanged). local mode is a no-op skip. inventory says count=0 in prod; dry-run will print "0 orphans found". working tree had unrelated WIP on src/lib/auth*, src/app/api/auth* and another plan's STATUS.md — left in place, not staged. |
| 3 | Pass 2: archive billing periods for soft-deleted groups | complete | 1 | phase-03-orphan-periods.md | 2026-05-03 | 2026-05-03 | implemented in scripts/db-cleanup.ts via mongoose models + raw collection writes (`archivedAt`/`archivalReason` not in BillingPeriod schema, `billing_periods_archived_for_inactive_groups` not in AuditEvent enum, both bypassed via `.collection.updateMany/insertOne` to keep schemas unchanged). local mode is a no-op skip. inventory says count=6 in prod (all under inactive group `69bac59addf7b647fbfdbde4`). open-period filters updated: `collectionWindowOpenFilter()` + `listUnpaidPeriodsWithStartBefore()` now exclude `archivedAt: null`. sqlite adapter intentionally untouched (mongo-only pass; no soft-deleted groups in local mode). working tree had unrelated WIP on src/lib/auth*, src/app/api/auth/n450s/callback, .env.example, and another plan's STATUS.md — left in place, not staged. |
| 4 | Pass 3: prune notification log entries older than retention window | complete | 1 | phase-04-old-notifications.md | 2026-05-03 | 2026-05-03 | implemented in scripts/db-cleanup.ts via Notification mongoose model; resolution order getSetting → env NOTIFICATION_RETENTION_DAYS → 365; `general.notificationRetentionDays` intentionally NOT registered in src/lib/settings/definitions.ts to honor the brief's "only if needed" allowlist note — getSetting returns null for unregistered key and we fall through to env/default. local mode is a no-op skip (sqlite stores notifications as JSON blobs requiring raw sqlite access; deferred until adapter support exists, so manual-verification "Local" step is not satisfied — note for phase-06 retro). audit event `old_notifications_pruned` written via raw `.collection.insertOne` (action not in AuditEvent enum). working tree had unrelated WIP on src/lib/auth*, src/app/api/auth/n450s/callback, .env.example, and another plan's STATUS.md — left in place, not staged. |
| 5 | Pass 4: rebuild stale indexes and run `compact` | complete | 2,3,4 | phase-05-indexes-and-compact.md | 2026-05-03 | 2026-05-03 | new `scripts/db-maintenance.ts` (separate from db-cleanup.ts; brief allowed either) — hand-curated expected index list mirrors all 8 schemas in src/models/*.ts. dry-run prints per-collection diff + sizes; `--apply` drops unexpected, creates missing, then runs `db.runCommand({compact})` per collection and reports `storageSize` delta as bytes reclaimed. row-count drift before/after is asserted. compact failures are surfaced per-collection (e.g. Atlas primary may refuse) and the script continues. local mode is a no-op skip. `pnpm db:maintenance` registered. lint clean (pre-existing repo errors are unrelated). manual local-mongo verification step deferred to phase 6 prod-log. working tree had unrelated WIP on src/lib/auth*, src/app/api/auth/n450s/callback, .env.example, and another plan's STATUS.md — left in place, not staged. |
| 6 | Production execution log + retro | pending | 2,3,4,5 | phase-06-prod-log.md | | | manual; final receipt |
