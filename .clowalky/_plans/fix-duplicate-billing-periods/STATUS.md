| ID | Phase | Status | Depends on | Brief | Started | Completed | Notes |
|----|-------|--------|------------|-------|---------|-----------|-------|
| 0 | Reproduce + characterize the bug | complete | — | phase-00-reproduce.md | 2026-05-03 | 2026-05-03 | bug confirmed: getPeriodDates uses local-time `new Date(year, month, day)`, so periodStart drifts across server TZ; reproduced via pre-seeded canonical-UTC row + cron tick at local-midnight boundary |
| 1 | Switch period date math to UTC | complete | 0 | phase-01-utc-date-math.md | 2026-05-03 | 2026-05-03 | UTC accessors in `createPeriodIfDue` + `getNextPeriodStart`; `getPeriodDates` now `Date.UTC`; `collection-window` uses `setUTCDate`/`getUTCDate`. Tests green under TZ=UTC, Europe/Athens, Pacific/Apia. Test file `periods.test.ts` had unrelated WIP touch-ups — left unstaged (not in this phase's allowlist). |
| 2 | Add a logical `(group, year, month)` dedup guard | complete | 1 | phase-02-logical-dedup-guard.md | 2026-05-03 | 2026-05-03 | `findExistingPeriodForMonth(store, groupId, year, monthIndex)` added in `periods.ts`; both `createPeriodIfDue` and `POST /api/groups/[groupId]/billing` now use it (POST is now idempotent — returns existing period instead of 409). `period_dedup_hit` audit event fires when candidate `periodStart` differs from stored. Brief allowlist had to be widened to `src/lib/storage/types.ts` (the `StorageAuditAction` union — without it the new audit value won't typecheck through `logAudit`). `periods.test.ts` mock needed `getPeriodsForGroup` + `logAudit` stubs to stay green; left unstaged for phase 3 to absorb (matches phase 1's WIP-handover pattern). |
| 3 | Add unit tests for boundary cases | complete | 1 | phase-03-tests.md | 2026-05-03 | 2026-05-03 | Absorbed phase-2's WIP on `periods.test.ts` (mock stubs + reproduction-test rewrite). New coverage: TZ matrix (UTC/Athens/Apia/LA) for the cron tick at the UTC month boundary, idempotency of repeat `createPeriodIfDue`, dedup audit emission, `getPeriodDates` TZ-invariance, leap-year Feb 29. New `route.test.ts` covers POST idempotency for both same-instant retries and same-month-different-instant races (asserts dedup audit fires/silences correctly). The brief asked for a `cycleDay=31 → clamp` test, but the production code currently overflows (`Date.UTC(2026,10,31)` → Dec 1) and clamping is out of phase-3 scope (tests-only); pinned the overflow with a comment marking the future enhancement. All 65 tests green under TZ=UTC, TZ=Asia/Athens, TZ=Pacific/Apia. `pnpm lint` shows only pre-existing unrelated failures (none in touched files). |
| 4 | Backfill / cleanup script for already-duplicated periods | complete | 1 | phase-04-cleanup-script.md | 2026-05-03 | 2026-05-03 | `scripts/cleanup-duplicate-billing-periods.ts` + `pnpm cleanup:dup-periods` (dry-run default; `--apply` opts in). Buckets non-archived periods by (group, UTC year+month), keeps oldest by `createdAt`, merges payments by `confirmed/waived > member_confirmed > overdue > pending` with confirmation-timestamp tiebreak, sets `archivedAt = new Date()` via raw collection driver (bypasses schema strict mode — no schema touch needed). One `period_duplicate_merged` audit per merge. Idempotent: re-runs skip `archivedAt: { $exists: true }` rows. Brief allowlist widened to `src/models/audit-event.ts` + `src/lib/storage/types.ts` to add the new audit action (same handover pattern as phase 2). Lint + 65/65 tests green. Apply-flow needs a real Mongo to verify end-to-end — phase 5 picks that up. |
| 5a | Production dry-run cleanup (agent-runnable) | blocked | 2,3,4 | phase-05a-prod-dry-run.md | 2026-05-03 | | harness sandbox denied `ssh root@135.181.153.99 docker exec ...` ("Read inside production via SSH+docker exec... requires explicit user approval naming the prod target"); brief assumed SSH-allow rule is in place but it is not. Operator must either grant the bash permission and rerun, or run the dry-run manually and paste output into phase-05a-results.md. |
| 5 | Production verification | blocked | 2,3,4,5a | phase-05-prod-verify.md | 2026-05-03 | | originally blocked. Split 2026-05-03 — extracted dry-run snapshot into 5a. This row still owns: deploy new image, two-cron-tick wait + re-run, CHANGELOG entry. Operator-driven (deploy + wait); the re-run dry-run can be agent-driven the same way as 5a. |
