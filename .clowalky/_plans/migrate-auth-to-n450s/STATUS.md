| ID | Phase | Status | Depends on | Brief | Started | Completed | Notes |
|----|-------|--------|------------|-------|---------|-----------|-------|
| 0 | Decide auth scope + register OAuth client | complete | — | phase-00-scope-and-client.md | 2026-05-03 | 2026-05-03 | OAuth client registration is operator-driven (separate repo + running auth service); decisions.md documents the procedure and target values. Secret stays out of repo. |
| 1 | Add OIDC config + JWKS verifier helpers | complete | 0 | phase-01-oidc-helpers.md | 2026-05-03 | 2026-05-03 | jose@^6.2.1 added; jwks.test.ts covers happy / wrong-iss / wrong-aud / expired / bad-signature. Pre-existing lint errors in unrelated files left untouched; tests in `src/lib/billing/*` are unrelated WIP and out of scope. |
| 2 | Implement n450s login + callback routes | complete | 1 | phase-02-login-callback.md | 2026-05-03 | 2026-05-03 | setSessionTokens left as a no-op stub in callback route — phase 3 wires real cookie writes. HMAC sign/verify helpers duplicated across login/callback (no shared module added since it would fall outside the brief's "Files to touch" allowlist). |
| 3 | Token storage + middleware refresh (sliding sessions) | complete | 2 | phase-03-token-refresh.md | 2026-05-03 | 2026-05-03 | Helper + middleware land here; brief's "Files to touch" omits the callback route, so the phase-2 setSessionTokens stub still no-ops — wiring it to call the new helper is required before sessions actually persist (move to phase 4 or a follow-up). Middleware enforces login redirect only on `/` and `/dashboard/*`; `/api/*` passes through and relies on the future `auth()` wrapper. |
| 4 | Rewrite `auth()` wrapper around n450s tokens (advanced mode) | complete | 3 | phase-04-auth-wrapper.md | 2026-05-03 | 2026-05-03 | advanced-mode `auth()` resolves sessions from `s5_at` via session-resolver (sub used as user.id with TODO until phase 5). NextAuth no longer invoked from auth(); `handlers` export kept so the legacy `/api/auth/[...nextauth]` route survives until phase 6 (route stub change is outside this brief's "Files to touch" allowlist). Phase-3 carryover still present: callback route's `setSessionTokens` remains a no-op stub, so login does not yet persist cookies — needs a follow-up before manual verification can pass end-to-end. Pre-existing tsc errors in `src/lib/authorization.test.ts` and unrelated lint errors left untouched. |
| 5 | User-row linking (`User.authIdentityId = sub`) | complete | 4 | phase-05-user-link.md | 2026-05-03 | 2026-05-03 | `User.authIdentityId` added (sparse unique) + plumbed through both adapters; n450s callback now resolves-or-creates the local user (sub → email-link → create). Migration script `scripts/link-existing-users-to-n450s.ts` calls `/admin/api/users-sync` with `N450S_ADMIN_TOKEN`, dry-run by default, idempotent on re-run. Pre-existing tsc errors in `src/lib/authorization.test.ts` and `group-card-actions.tsx` left untouched. Carryover: `session-resolver.ts` still surfaces `sub` as `user.id` (its phase-5 TODO) — outside this brief's "Files to touch", so the local `User._id` swap belongs to phase 6/7 wiring. Phase-3 callback-cookie no-op also still present. |
| 6 | Remove Credentials + Magic-invite providers | pending | 4,5 | phase-06-deprecate-providers.md | | | invite flow uses n450s invite codes |
| 7 | Login UI rewrite (n450s redirect, Google via n450s, fallback states) | pending | 4 | phase-07-login-ui.md | | | parallel with 5/6 if no overlap |
| 8 | Tests + integration smoke | pending | 5,6,7 | phase-08-tests.md | | | |
| 9 | Production cutover + rollback plan | pending | 8 | phase-09-cutover.md | | | manual; documented procedure |
