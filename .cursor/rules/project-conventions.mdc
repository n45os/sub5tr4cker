---
description: Project-wide conventions for SubsTrack
globs: **/*
alwaysApply: true
---

# SubsTrack Conventions

## Project Context

SubsTrack is an open-source Next.js app for managing shared subscriptions. Read `AGENTS.md` at the project root for the full directory map and architecture.

Detailed docs:
- `docs/PLAN.md` — architecture plan, feature roadmap, billing modes
- `docs/data-models.md` — Mongoose schema documentation
- `docs/api-design.md` — API route reference
- Local mode: `s54r` CLI, SQLite storage, token auth — see CLI commands in `AGENTS.md`

## Package Manager

- Use `pnpm` for all dependency and script commands in this repo
- Prefer `pnpm install`, `pnpm dev`, `pnpm build`, `pnpm test`
- Avoid mixing package managers (`npm` + `pnpm`) in the same branch

## Troubleshooting Guardrails

- If test startup fails with `Cannot find native binding` and `@rolldown/binding-darwin-arm64`, reinstall dependencies with `pnpm install` and rerun using `pnpm` commands
- If `MongoDBAdapter` shows a type mismatch against `MongoClient` after switching package manager, add/keep a direct `mongodb@^6` dependency to satisfy the adapter peer and align types
- For strict TypeScript route code, explicitly type callback parameters in array methods on mongoose arrays (`find`, `map`, `filter`, `some`, `every`) to avoid `Parameter 'x' implicitly has an 'any' type`
- Avoid very large inline object types directly in callback signatures inside route files; move them to a named `type` to prevent Turbopack parser errors like `Expected ',', got ';'`

## Code Style

- Comments: start with lowercase, no trailing period (e.g., `// calculate member share`)
- Use TypeScript strict mode everywhere
- Prefer `const` over `let`, never use `var`
- Use named exports, not default exports (exception: Next.js pages)
- Import alias: `@/*` maps to `src/*`

## API Routes

- Always validate request body with Zod before processing
- Check auth with `auth()` from `@/lib/auth` on protected routes
- Return `{ data: ... }` on success
- Return `{ error: { code, message, details? } }` on failure
- Use standard HTTP status codes

## Database

- Mongoose models in `src/models/`, re-exported from `src/models/index.ts`
- Use `const store = await db()` from `@/lib/storage` in API routes (works for both SQLite and MongoDB)
- Mongoose validation is the second layer in advanced mode; Zod is the primary validation for all modes

## Components

- Use shadcn/ui for primitive components (`src/components/ui/`)
- Feature-specific components in `src/components/features/<feature>/`
- Layout components in `src/components/layout/`
- Prefer server components, use `'use client'` only when needed

## Environment

- All secrets via environment variables, never hardcode
- Reference `.env.example` for the full variable list
- Use `process.env.VARIABLE` with runtime checks

## Notifications

- Email templates in `src/lib/email/templates/` as React Email components
- Telegram helpers in `src/lib/telegram/`
- Unified dispatch through `src/lib/notifications/service.ts`
